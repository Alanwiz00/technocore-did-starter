{
  "name": "technocore-did-starter",
  "version": "1.10.0",
  "private": true,
  "description": "Operational scripts for the Technocore Python agent",
  "scripts": {
    "start": "node scripts/python.js technocore_runner.py",
    "test": "node scripts/python.js -m unittest discover -s tests -v",
    "check": "node scripts/python.js -m compileall -q technocore_agent.py technocore_runner.py tests && node scripts/python.js -m unittest discover -s tests -v"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@flop-labs/tclk": "^0.1.0",
    "@flop-labs/tclk-mcp": "^0.1.0"
  }
}
